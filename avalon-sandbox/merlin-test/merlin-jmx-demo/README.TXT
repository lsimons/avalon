
A set of JMX related components, including:

  - a JMX server
  - a HTTP connector
  - a JRMP connector
  - a series of manageble components

To execute the demo:

  $ maven clean jar
  $ merlin target\merlin-jmx-demo-1.0.jar

Launch a JMX client (using either HTTP or RMI to see the components).
